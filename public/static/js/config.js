var STATUS_N = 0;
var STATUS_Y = 1;
var STATUS_B = 2;

var APPLY_P = 1; // 已通过
var APPLY_I = 2; // 申请中
var APPLY_B = 3; // 已驳回

var ORDER_S_W = 1; // 未支付
var ORDER_S_P = 2; // 已支付
var ORDER_S_C = 3; // 已退款